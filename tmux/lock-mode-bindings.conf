# Lock Mode - Comprehensive Key Bindings
# Auto-generated by generate-lock-mode-bindings.sh
# This file contains bindings for 276 keys

bind -T locked 'a' send-keys 'a' \; switch-client -T locked
bind -T locked 'b' send-keys 'b' \; switch-client -T locked
bind -T locked 'c' send-keys 'c' \; switch-client -T locked
bind -T locked 'd' send-keys 'd' \; switch-client -T locked
bind -T locked 'e' send-keys 'e' \; switch-client -T locked
bind -T locked 'f' send-keys 'f' \; switch-client -T locked
bind -T locked 'g' send-keys 'g' \; switch-client -T locked
bind -T locked 'h' send-keys 'h' \; switch-client -T locked
bind -T locked 'i' send-keys 'i' \; switch-client -T locked
bind -T locked 'j' send-keys 'j' \; switch-client -T locked
bind -T locked 'k' send-keys 'k' \; switch-client -T locked
bind -T locked 'l' send-keys 'l' \; switch-client -T locked
bind -T locked 'm' send-keys 'm' \; switch-client -T locked
bind -T locked 'n' send-keys 'n' \; switch-client -T locked
bind -T locked 'o' send-keys 'o' \; switch-client -T locked
bind -T locked 'p' send-keys 'p' \; switch-client -T locked
bind -T locked 'q' send-keys 'q' \; switch-client -T locked
bind -T locked 'r' send-keys 'r' \; switch-client -T locked
bind -T locked 's' send-keys 's' \; switch-client -T locked
bind -T locked 't' send-keys 't' \; switch-client -T locked
bind -T locked 'u' send-keys 'u' \; switch-client -T locked
bind -T locked 'v' send-keys 'v' \; switch-client -T locked
bind -T locked 'w' send-keys 'w' \; switch-client -T locked
bind -T locked 'x' send-keys 'x' \; switch-client -T locked
bind -T locked 'y' send-keys 'y' \; switch-client -T locked
bind -T locked 'z' send-keys 'z' \; switch-client -T locked
bind -T locked 'A' send-keys 'A' \; switch-client -T locked
bind -T locked 'B' send-keys 'B' \; switch-client -T locked
bind -T locked 'C' send-keys 'C' \; switch-client -T locked
bind -T locked 'D' send-keys 'D' \; switch-client -T locked
bind -T locked 'E' send-keys 'E' \; switch-client -T locked
bind -T locked 'F' send-keys 'F' \; switch-client -T locked
bind -T locked 'G' send-keys 'G' \; switch-client -T locked
bind -T locked 'H' send-keys 'H' \; switch-client -T locked
bind -T locked 'I' send-keys 'I' \; switch-client -T locked
bind -T locked 'J' send-keys 'J' \; switch-client -T locked
bind -T locked 'K' send-keys 'K' \; switch-client -T locked
bind -T locked 'L' send-keys 'L' \; switch-client -T locked
bind -T locked 'M' send-keys 'M' \; switch-client -T locked
bind -T locked 'N' send-keys 'N' \; switch-client -T locked
bind -T locked 'O' send-keys 'O' \; switch-client -T locked
bind -T locked 'P' send-keys 'P' \; switch-client -T locked
bind -T locked 'Q' send-keys 'Q' \; switch-client -T locked
bind -T locked 'R' send-keys 'R' \; switch-client -T locked
bind -T locked 'S' send-keys 'S' \; switch-client -T locked
bind -T locked 'T' send-keys 'T' \; switch-client -T locked
bind -T locked 'U' send-keys 'U' \; switch-client -T locked
bind -T locked 'V' send-keys 'V' \; switch-client -T locked
bind -T locked 'W' send-keys 'W' \; switch-client -T locked
bind -T locked 'X' send-keys 'X' \; switch-client -T locked
bind -T locked 'Y' send-keys 'Y' \; switch-client -T locked
bind -T locked 'Z' send-keys 'Z' \; switch-client -T locked
bind -T locked 'C-a' send-keys 'C-a' \; switch-client -T locked
bind -T locked 'C-b' send-keys 'C-b' \; switch-client -T locked
bind -T locked 'C-c' send-keys 'C-c' \; switch-client -T locked
bind -T locked 'C-e' send-keys 'C-e' \; switch-client -T locked
bind -T locked 'C-f' send-keys 'C-f' \; switch-client -T locked
bind -T locked 'C-h' send-keys 'C-h' \; switch-client -T locked
bind -T locked 'C-i' send-keys 'C-i' \; switch-client -T locked
bind -T locked 'C-j' send-keys 'C-j' \; switch-client -T locked
bind -T locked 'C-k' send-keys 'C-k' \; switch-client -T locked
bind -T locked 'C-l' send-keys 'C-l' \; switch-client -T locked
bind -T locked 'C-m' send-keys 'C-m' \; switch-client -T locked
bind -T locked 'C-n' send-keys 'C-n' \; switch-client -T locked
bind -T locked 'C-o' send-keys 'C-o' \; switch-client -T locked
bind -T locked 'C-p' send-keys 'C-p' \; switch-client -T locked
bind -T locked 'C-q' send-keys 'C-q' \; switch-client -T locked
bind -T locked 'C-r' send-keys 'C-r' \; switch-client -T locked
bind -T locked 'C-s' send-keys 'C-s' \; switch-client -T locked
bind -T locked 'C-t' send-keys 'C-t' \; switch-client -T locked
bind -T locked 'C-u' send-keys 'C-u' \; switch-client -T locked
bind -T locked 'C-v' send-keys 'C-v' \; switch-client -T locked
bind -T locked 'C-w' send-keys 'C-w' \; switch-client -T locked
bind -T locked 'C-x' send-keys 'C-x' \; switch-client -T locked
bind -T locked 'C-y' send-keys 'C-y' \; switch-client -T locked
bind -T locked 'M-a' send-keys 'M-a' \; switch-client -T locked
bind -T locked 'M-b' send-keys 'M-b' \; switch-client -T locked
bind -T locked 'M-c' send-keys 'M-c' \; switch-client -T locked
bind -T locked 'M-d' send-keys 'M-d' \; switch-client -T locked
bind -T locked 'M-e' send-keys 'M-e' \; switch-client -T locked
bind -T locked 'M-f' send-keys 'M-f' \; switch-client -T locked
bind -T locked 'M-g' send-keys 'M-g' \; switch-client -T locked
bind -T locked 'M-h' send-keys 'M-h' \; switch-client -T locked
bind -T locked 'M-i' send-keys 'M-i' \; switch-client -T locked
bind -T locked 'M-j' send-keys 'M-j' \; switch-client -T locked
bind -T locked 'M-k' send-keys 'M-k' \; switch-client -T locked
bind -T locked 'M-l' send-keys 'M-l' \; switch-client -T locked
bind -T locked 'M-m' send-keys 'M-m' \; switch-client -T locked
bind -T locked 'M-n' send-keys 'M-n' \; switch-client -T locked
bind -T locked 'M-o' send-keys 'M-o' \; switch-client -T locked
bind -T locked 'M-p' send-keys 'M-p' \; switch-client -T locked
bind -T locked 'M-q' send-keys 'M-q' \; switch-client -T locked
bind -T locked 'M-r' send-keys 'M-r' \; switch-client -T locked
bind -T locked 'M-s' send-keys 'M-s' \; switch-client -T locked
bind -T locked 'M-t' send-keys 'M-t' \; switch-client -T locked
bind -T locked 'M-u' send-keys 'M-u' \; switch-client -T locked
bind -T locked 'M-v' send-keys 'M-v' \; switch-client -T locked
bind -T locked 'M-w' send-keys 'M-w' \; switch-client -T locked
bind -T locked 'M-x' send-keys 'M-x' \; switch-client -T locked
bind -T locked 'M-y' send-keys 'M-y' \; switch-client -T locked
bind -T locked 'M-z' send-keys 'M-z' \; switch-client -T locked
bind -T locked 'M-A' send-keys 'M-A' \; switch-client -T locked
bind -T locked 'M-B' send-keys 'M-B' \; switch-client -T locked
bind -T locked 'M-C' send-keys 'M-C' \; switch-client -T locked
bind -T locked 'M-D' send-keys 'M-D' \; switch-client -T locked
bind -T locked 'M-E' send-keys 'M-E' \; switch-client -T locked
bind -T locked 'M-F' send-keys 'M-F' \; switch-client -T locked
bind -T locked 'M-G' send-keys 'M-G' \; switch-client -T locked
bind -T locked 'M-H' send-keys 'M-H' \; switch-client -T locked
bind -T locked 'M-I' send-keys 'M-I' \; switch-client -T locked
bind -T locked 'M-J' send-keys 'M-J' \; switch-client -T locked
bind -T locked 'M-K' send-keys 'M-K' \; switch-client -T locked
bind -T locked 'M-L' send-keys 'M-L' \; switch-client -T locked
bind -T locked 'M-M' send-keys 'M-M' \; switch-client -T locked
bind -T locked 'M-N' send-keys 'M-N' \; switch-client -T locked
bind -T locked 'M-O' send-keys 'M-O' \; switch-client -T locked
bind -T locked 'M-P' send-keys 'M-P' \; switch-client -T locked
bind -T locked 'M-Q' send-keys 'M-Q' \; switch-client -T locked
bind -T locked 'M-R' send-keys 'M-R' \; switch-client -T locked
bind -T locked 'M-S' send-keys 'M-S' \; switch-client -T locked
bind -T locked 'M-T' send-keys 'M-T' \; switch-client -T locked
bind -T locked 'M-U' send-keys 'M-U' \; switch-client -T locked
bind -T locked 'M-V' send-keys 'M-V' \; switch-client -T locked
bind -T locked 'M-W' send-keys 'M-W' \; switch-client -T locked
bind -T locked 'M-X' send-keys 'M-X' \; switch-client -T locked
bind -T locked 'M-Y' send-keys 'M-Y' \; switch-client -T locked
bind -T locked 'M-Z' send-keys 'M-Z' \; switch-client -T locked
bind -T locked '0' send-keys '0' \; switch-client -T locked
bind -T locked '1' send-keys '1' \; switch-client -T locked
bind -T locked '2' send-keys '2' \; switch-client -T locked
bind -T locked '3' send-keys '3' \; switch-client -T locked
bind -T locked '4' send-keys '4' \; switch-client -T locked
bind -T locked '5' send-keys '5' \; switch-client -T locked
bind -T locked '6' send-keys '6' \; switch-client -T locked
bind -T locked '7' send-keys '7' \; switch-client -T locked
bind -T locked '8' send-keys '8' \; switch-client -T locked
bind -T locked '9' send-keys '9' \; switch-client -T locked
bind -T locked 'M-0' send-keys 'M-0' \; switch-client -T locked
bind -T locked 'M-1' send-keys 'M-1' \; switch-client -T locked
bind -T locked 'M-2' send-keys 'M-2' \; switch-client -T locked
bind -T locked 'M-3' send-keys 'M-3' \; switch-client -T locked
bind -T locked 'M-4' send-keys 'M-4' \; switch-client -T locked
bind -T locked 'M-5' send-keys 'M-5' \; switch-client -T locked
bind -T locked 'M-6' send-keys 'M-6' \; switch-client -T locked
bind -T locked 'M-7' send-keys 'M-7' \; switch-client -T locked
bind -T locked 'M-8' send-keys 'M-8' \; switch-client -T locked
bind -T locked 'M-9' send-keys 'M-9' \; switch-client -T locked
bind -T locked 'F1' send-keys 'F1' \; switch-client -T locked
bind -T locked 'S-F1' send-keys 'S-F1' \; switch-client -T locked
bind -T locked 'C-F1' send-keys 'C-F1' \; switch-client -T locked
bind -T locked 'M-F1' send-keys 'M-F1' \; switch-client -T locked
bind -T locked 'F2' send-keys 'F2' \; switch-client -T locked
bind -T locked 'S-F2' send-keys 'S-F2' \; switch-client -T locked
bind -T locked 'C-F2' send-keys 'C-F2' \; switch-client -T locked
bind -T locked 'M-F2' send-keys 'M-F2' \; switch-client -T locked
bind -T locked 'F3' send-keys 'F3' \; switch-client -T locked
bind -T locked 'S-F3' send-keys 'S-F3' \; switch-client -T locked
bind -T locked 'C-F3' send-keys 'C-F3' \; switch-client -T locked
bind -T locked 'M-F3' send-keys 'M-F3' \; switch-client -T locked
bind -T locked 'F4' send-keys 'F4' \; switch-client -T locked
bind -T locked 'S-F4' send-keys 'S-F4' \; switch-client -T locked
bind -T locked 'C-F4' send-keys 'C-F4' \; switch-client -T locked
bind -T locked 'M-F4' send-keys 'M-F4' \; switch-client -T locked
bind -T locked 'F5' send-keys 'F5' \; switch-client -T locked
bind -T locked 'S-F5' send-keys 'S-F5' \; switch-client -T locked
bind -T locked 'C-F5' send-keys 'C-F5' \; switch-client -T locked
bind -T locked 'M-F5' send-keys 'M-F5' \; switch-client -T locked
bind -T locked 'F6' send-keys 'F6' \; switch-client -T locked
bind -T locked 'S-F6' send-keys 'S-F6' \; switch-client -T locked
bind -T locked 'C-F6' send-keys 'C-F6' \; switch-client -T locked
bind -T locked 'M-F6' send-keys 'M-F6' \; switch-client -T locked
bind -T locked 'F7' send-keys 'F7' \; switch-client -T locked
bind -T locked 'S-F7' send-keys 'S-F7' \; switch-client -T locked
bind -T locked 'C-F7' send-keys 'C-F7' \; switch-client -T locked
bind -T locked 'M-F7' send-keys 'M-F7' \; switch-client -T locked
bind -T locked 'F8' send-keys 'F8' \; switch-client -T locked
bind -T locked 'S-F8' send-keys 'S-F8' \; switch-client -T locked
bind -T locked 'C-F8' send-keys 'C-F8' \; switch-client -T locked
bind -T locked 'M-F8' send-keys 'M-F8' \; switch-client -T locked
bind -T locked 'F9' send-keys 'F9' \; switch-client -T locked
bind -T locked 'S-F9' send-keys 'S-F9' \; switch-client -T locked
bind -T locked 'C-F9' send-keys 'C-F9' \; switch-client -T locked
bind -T locked 'M-F9' send-keys 'M-F9' \; switch-client -T locked
bind -T locked 'F10' send-keys 'F10' \; switch-client -T locked
bind -T locked 'S-F10' send-keys 'S-F10' \; switch-client -T locked
bind -T locked 'C-F10' send-keys 'C-F10' \; switch-client -T locked
bind -T locked 'M-F10' send-keys 'M-F10' \; switch-client -T locked
bind -T locked 'F11' send-keys 'F11' \; switch-client -T locked
bind -T locked 'S-F11' send-keys 'S-F11' \; switch-client -T locked
bind -T locked 'C-F11' send-keys 'C-F11' \; switch-client -T locked
bind -T locked 'M-F11' send-keys 'M-F11' \; switch-client -T locked
bind -T locked 'F12' send-keys 'F12' \; switch-client -T locked
bind -T locked 'S-F12' send-keys 'S-F12' \; switch-client -T locked
bind -T locked 'C-F12' send-keys 'C-F12' \; switch-client -T locked
bind -T locked 'M-F12' send-keys 'M-F12' \; switch-client -T locked
bind -T locked 'Up' send-keys 'Up' \; switch-client -T locked
bind -T locked 'S-Up' send-keys 'S-Up' \; switch-client -T locked
bind -T locked 'C-Up' send-keys 'C-Up' \; switch-client -T locked
bind -T locked 'M-Up' send-keys 'M-Up' \; switch-client -T locked
bind -T locked 'Down' send-keys 'Down' \; switch-client -T locked
bind -T locked 'S-Down' send-keys 'S-Down' \; switch-client -T locked
bind -T locked 'C-Down' send-keys 'C-Down' \; switch-client -T locked
bind -T locked 'M-Down' send-keys 'M-Down' \; switch-client -T locked
bind -T locked 'Left' send-keys 'Left' \; switch-client -T locked
bind -T locked 'S-Left' send-keys 'S-Left' \; switch-client -T locked
bind -T locked 'C-Left' send-keys 'C-Left' \; switch-client -T locked
bind -T locked 'M-Left' send-keys 'M-Left' \; switch-client -T locked
bind -T locked 'Right' send-keys 'Right' \; switch-client -T locked
bind -T locked 'S-Right' send-keys 'S-Right' \; switch-client -T locked
bind -T locked 'C-Right' send-keys 'C-Right' \; switch-client -T locked
bind -T locked 'M-Right' send-keys 'M-Right' \; switch-client -T locked
bind -T locked 'Home' send-keys 'Home' \; switch-client -T locked
bind -T locked 'S-Home' send-keys 'S-Home' \; switch-client -T locked
bind -T locked 'C-Home' send-keys 'C-Home' \; switch-client -T locked
bind -T locked 'M-Home' send-keys 'M-Home' \; switch-client -T locked
bind -T locked 'End' send-keys 'End' \; switch-client -T locked
bind -T locked 'S-End' send-keys 'S-End' \; switch-client -T locked
bind -T locked 'C-End' send-keys 'C-End' \; switch-client -T locked
bind -T locked 'M-End' send-keys 'M-End' \; switch-client -T locked
bind -T locked 'PageUp' send-keys 'PageUp' \; switch-client -T locked
bind -T locked 'S-PageUp' send-keys 'S-PageUp' \; switch-client -T locked
bind -T locked 'C-PageUp' send-keys 'C-PageUp' \; switch-client -T locked
bind -T locked 'M-PageUp' send-keys 'M-PageUp' \; switch-client -T locked
bind -T locked 'PageDown' send-keys 'PageDown' \; switch-client -T locked
bind -T locked 'S-PageDown' send-keys 'S-PageDown' \; switch-client -T locked
bind -T locked 'C-PageDown' send-keys 'C-PageDown' \; switch-client -T locked
bind -T locked 'M-PageDown' send-keys 'M-PageDown' \; switch-client -T locked
bind -T locked 'Insert' send-keys 'Insert' \; switch-client -T locked
bind -T locked 'S-Insert' send-keys 'S-Insert' \; switch-client -T locked
bind -T locked 'C-Insert' send-keys 'C-Insert' \; switch-client -T locked
bind -T locked 'M-Insert' send-keys 'M-Insert' \; switch-client -T locked
bind -T locked 'Delete' send-keys 'Delete' \; switch-client -T locked
bind -T locked 'S-Delete' send-keys 'S-Delete' \; switch-client -T locked
bind -T locked 'C-Delete' send-keys 'C-Delete' \; switch-client -T locked
bind -T locked 'M-Delete' send-keys 'M-Delete' \; switch-client -T locked
bind -T locked 'NPage' send-keys 'NPage' \; switch-client -T locked
bind -T locked 'S-NPage' send-keys 'S-NPage' \; switch-client -T locked
bind -T locked 'C-NPage' send-keys 'C-NPage' \; switch-client -T locked
bind -T locked 'M-NPage' send-keys 'M-NPage' \; switch-client -T locked
bind -T locked 'PPage' send-keys 'PPage' \; switch-client -T locked
bind -T locked 'S-PPage' send-keys 'S-PPage' \; switch-client -T locked
bind -T locked 'C-PPage' send-keys 'C-PPage' \; switch-client -T locked
bind -T locked 'M-PPage' send-keys 'M-PPage' \; switch-client -T locked
bind -T locked '!' send-keys '!' \; switch-client -T locked
bind -T locked '@' send-keys '@' \; switch-client -T locked
bind -T locked '#' send-keys '#' \; switch-client -T locked
bind -T locked '$' send-keys '$' \; switch-client -T locked
bind -T locked '%' send-keys '%' \; switch-client -T locked
bind -T locked '^' send-keys '^' \; switch-client -T locked
bind -T locked '&' send-keys '&' \; switch-client -T locked
bind -T locked '*' send-keys '*' \; switch-client -T locked
bind -T locked '(' send-keys '(' \; switch-client -T locked
bind -T locked ')' send-keys ')' \; switch-client -T locked
bind -T locked '-' send-keys '-' \; switch-client -T locked
bind -T locked '=' send-keys '=' \; switch-client -T locked
bind -T locked '[' send-keys '[' \; switch-client -T locked
bind -T locked ']' send-keys ']' \; switch-client -T locked
bind -T locked '{' send-keys '{' \; switch-client -T locked
bind -T locked '}' send-keys '}' \; switch-client -T locked
bind -T locked ';' send-keys ';' \; switch-client -T locked
bind -T locked ':' send-keys ':' \; switch-client -T locked
bind -T locked ''' send-keys ''' \; switch-client -T locked
bind -T locked '"' send-keys '"' \; switch-client -T locked
bind -T locked ',' send-keys ',' \; switch-client -T locked
bind -T locked '.' send-keys '.' \; switch-client -T locked
bind -T locked '?' send-keys '?' \; switch-client -T locked
bind -T locked '/' send-keys '/' \; switch-client -T locked
bind -T locked '|' send-keys '|' \; switch-client -T locked
bind -T locked ' ' send-keys ' ' \; switch-client -T locked
bind -T locked 'Tab' send-keys 'Tab' \; switch-client -T locked
bind -T locked 'Enter' send-keys 'Enter' \; switch-client -T locked
bind -T locked 'BSpace' send-keys 'BSpace' \; switch-client -T locked
bind -T locked 'Escape' send-keys 'Escape' \; switch-client -T locked

# End of comprehensive lock mode bindings
